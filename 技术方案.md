# codex-notify 技术方案

## 1. 目标

`codex-notify` 是一个运行在 Windows 上的本地通知桥接程序，用于接收外部程序通过 shell 传入的 JSON 事件，并将其转换为 Windows 系统通知。

本项目的目标不是只处理某一种固定事件，而是作为一个长期可用的通用事件通知器，满足以下要求：

- 支持外部脚本以 JSON 形式调用
- 支持已知事件类型的定制化通知
- 支持未知事件类型的兜底通知
- 支持自动落盘日志
- 适合作为长期本地工具持续使用
- 后续可扩展为更多通知渠道，而不破坏现有调用方式

## 2. 典型输入

外部程序会传入类似如下 JSON：

```json
{
  "type": "agent-turn-complete",
  "thread-id": "019d8ae2-9946-7c80-836e-dc95d0508c00",
  "turn-id": "019d8b11-801e-74d3-a44d-80a82b980347",
  "cwd": "E:\\wang\\w-server",
  "client": "codex-tui",
  "input-messages": [
    "帮我移除user模块中的update接口",
    "codex resume 019d8b10-ef46-7b12-ae48-57a5e52cddbe"
  ],
  "last-assistant-message": "当前会话里没有我创建过的子 agent，因此不能对 `019d8b10-ef46-7b12-ae48-57a5e52cddbe` 执行 `resume`。\n\n我这边已经在处理中..."
}
```

该结构未来可能新增字段，也可能出现新的 `type`。因此程序必须具备前向兼容能力，不能因为未识别字段或未知类型而报错退出。

## 3. 总体设计

程序采用四层结构：

1. 输入层
负责读取外部传入的 JSON。

2. 事件层
负责解析公共字段，并保留完整原始负载。

3. 渲染层
负责将不同事件转换为统一的通知对象。

4. 发送层
负责调用 Windows Toast API 发送通知。

这样设计的核心价值是解耦：

- 输入协议变化时，不影响通知发送实现
- 通知库替换时，不影响事件解析逻辑
- 新增事件类型时，不影响现有调用方式

## 4. 输入协议设计

### 4.1 推荐输入方式

主输入方式采用 `stdin`，兼容 `--payload` 参数。

推荐：

```powershell
$payload | codex-notify.exe
```

兼容：

```powershell
codex-notify.exe --payload '{"type":"agent-turn-complete", ...}'
```

选择 `stdin` 作为主入口的原因：

- 最不容易受到 PowerShell 和 cmd 的引号、转义规则影响
- 更适合传输多行文本和较长 JSON
- 外部程序集成成本更低

### 4.2 未来可扩展输入方式

后续可扩展：

- `--file <path>`，从文件读取 JSON
- 监听命名管道
- 监听本地 HTTP 端口

第一阶段不建议同时实现，优先把 CLI 单次调用模式做稳定。

## 5. 事件模型设计

### 5.1 通用事件信封

程序内部统一解析为 `EventEnvelope`：

```go
type EventEnvelope struct {
    Type     string
    ThreadID string
    TurnID   string
    Cwd      string
    Client   string

    Raw     map[string]any
    RawJSON []byte
}
```

说明：

- `Type`、`ThreadID`、`TurnID`、`Cwd`、`Client` 是固定公共字段
- `Raw` 保存完整 JSON 的对象结构
- `RawJSON` 保存原始字节，用于调试、日志和未知类型兜底

### 5.2 解析原则

- 已知公共字段尽量解析
- 缺失公共字段时不直接失败
- 未知字段必须保留
- 未知 `type` 不能报错，只进入默认渲染逻辑

### 5.3 未知类型支持策略

未知类型分为三类处理：

1. 已知类型
存在专用 renderer，例如 `agent-turn-complete`。

2. 半已知类型
类型不认识，但可以从 payload 中猜测 `message`、`summary`、`text`、`status` 等常见字段。

3. 完全未知类型
使用默认模板，标题显示 `type`，正文显示关键字段摘要或压缩后的 JSON。

## 6. 通知模型设计

渲染层输出统一的 `Notification`：

```go
type Notification struct {
    AppID   string
    Title   string
    Message string
    Icon    string
    Group   string
    Tag     string
    Silent  bool
}
```

说明：

- `AppID` 用于在 Windows 中标识应用
- `Title` 为通知标题
- `Message` 为通知正文
- `Icon` 为可选图标路径
- `Group` 和 `Tag` 用于分组、替换和去重
- `Silent` 控制是否静默

## 7. 事件到通知的渲染规则

### 7.1 已知事件：`agent-turn-complete`

建议渲染规则：

- 标题：`Codex 已完成一轮处理`
- 分组：`thread-id`
- 标签：`turn-id`
- 副信息来源：从 `cwd` 中提取项目目录名，例如 `w-server`

正文建议分两段：

- 第一段：优先取 `input-messages[0]`
- 第二段：取 `last-assistant-message` 的摘要文本

如果正文过长，应执行清洗和截断。

### 7.2 未知事件

默认渲染规则：

- 标题：`Codex 事件: <type>`
- 分组：优先 `thread-id`，否则为空
- 标签：优先 `turn-id`，否则用内容摘要 hash
- 正文：
  - 优先读取 `message`
  - 再尝试 `summary`
  - 再尝试 `text`
  - 再尝试 `status`
  - 都不存在时，显示压缩后的关键字段摘要

### 7.3 文本清洗规则

所有进入通知正文的文本应经过统一清洗：

- 去掉 Markdown 反引号
- 将多个空行压缩为一个空格或一个换行
- 去掉多余首尾空白
- 限制最大长度，例如 160 到 240 字符
- 超出后追加 `...`

## 8. Windows 通知实现选型

### 8.1 选型结论

发送层采用 `github.com/go-toast/toast` 作为 Windows Toast 适配库。

选择理由：

- 适合本项目这种本地 CLI 工具
- 依赖轻，易于接入
- 足以覆盖标题、正文、图标、基础动作等常见 toast 需求
- 可作为发送层实现，而上层事件逻辑不直接耦合到该库

### 8.2 使用原则

不要在业务逻辑里直接操作 `toast.Notification`，而是通过本项目自己的 `Notifier` 接口隔离：

```go
type Notifier interface {
    Send(ctx context.Context, n Notification) error
}
```

再由 `internal/notify/windows` 使用 `go-toast/toast` 实现该接口。

这样做的目的：

- 以后更换通知库时不影响上层
- 未来接入其它通知渠道时不需要重写事件渲染层

### 8.3 能力边界

`go-toast/toast` 适合“发通知”的需求，不适合一开始就承担复杂通知平台能力。当前项目应避免将以下能力作为第一阶段目标：

- 非常复杂的交互式通知
- 深度依赖通知点击回调的工作流
- 超细粒度的通知生命周期管理

第一阶段目标是稳定、可长期使用、可扩展，而不是一次性把所有高级能力做全。

## 9. 去重与替换策略

长期使用时必须避免通知刷屏，因此需要本地去重。

### 9.1 去重主键

优先级如下：

1. `turn-id`
2. `thread-id + type + 内容摘要 hash`
3. 完整 payload hash

### 9.2 去重行为

- 相同 `turn-id` 的事件只通知一次
- 同线程下的后续状态更新可以使用相同 `group`
- 对可替换场景使用相同 `tag`，避免通知中心堆积过多重复项

### 9.3 状态存储

去重状态建议落盘到：

`%LOCALAPPDATA%\codex-notify\state\dedupe.json`

也可以拆分为轻量键值形式，避免单文件长期增长过快。

## 10. 自动落盘日志方案

### 10.1 选型结论

日志方案采用：

- 主日志库：`github.com/rs/zerolog`
- 文件滚动库：`gopkg.in/natefinch/lumberjack.v2`

选择理由：

- `zerolog` 适合结构化 JSON 日志
- 性能高，API 简洁
- `lumberjack` 专注于日志文件轮转，适合与 `io.Writer` 风格日志库组合
- 相比 `logrus` 更适合新项目，且 `logrus` 已处于维护模式

### 10.2 日志输出策略

默认同时输出到：

- `stderr`
- 日志文件

这样做的作用：

- 外部调用脚本可以直接看到错误输出
- 程序也能长期保存本地日志，便于排查问题

### 10.3 推荐日志目录

日志落盘目录建议：

`%LOCALAPPDATA%\codex-notify\logs\app.log`

状态文件目录建议：

`%LOCALAPPDATA%\codex-notify\state\`

后续配置文件目录建议：

`%LOCALAPPDATA%\codex-notify\config\`

### 10.4 日志内容建议

每条日志应尽量结构化，至少包含：

- 时间
- 级别
- 事件类型
- `thread-id`
- `turn-id`
- `client`
- `cwd`
- 结果状态
- 错误信息

示例字段：

```json
{
  "level": "info",
  "type": "agent-turn-complete",
  "thread_id": "019d8ae2-9946-7c80-836e-dc95d0508c00",
  "turn_id": "019d8b11-801e-74d3-a44d-80a82b980347",
  "cwd": "E:\\wang\\w-server",
  "msg": "notification sent"
}
```

### 10.5 日志轮转建议

建议默认策略：

- 单文件最大 10 MB
- 保留 7 个历史文件
- 保留 30 天
- 启用旧日志压缩

### 10.6 多进程边界

`lumberjack` 更适合单进程连续写同一个日志文件。若未来存在多个 `codex-notify.exe` 实例高频并发写同一文件，可能出现竞争风险。

第一阶段可接受该限制；若后续并发明显增加，可考虑：

- 按日期分文件
- 按进程分文件
- 改为更适合多进程的日志聚合方式

## 11. CLI 设计

建议保留以下命令行行为：

### 11.1 默认行为

无参数时，优先读取 `stdin`。

### 11.2 可选参数

- `--payload <json>`
- `--file <path>`
- `--verbose`
- `--silent`

第一阶段至少支持：

- 无参数读取 `stdin`
- `--payload`
- `--verbose`

### 11.3 退出码约定

- `0`：成功，或事件被正常忽略
- `1`：通知发送失败
- `2`：输入 JSON 非法
- `3`：程序初始化失败，例如日志目录不可创建

## 12. 配置策略

为保证长期可维护性，建议从结构上预留配置能力，即使第一阶段先写死默认值。

未来可配置项包括：

- AppID
- 默认图标路径
- 文本截断长度
- 是否静默
- 日志等级
- 日志路径
- 去重窗口
- 各种事件类型的模板规则

建议初期配置文件位置：

`%LOCALAPPDATA%\codex-notify\config\config.json`

## 13. 推荐目录结构

建议采用如下结构：

```text
cmd/
  codex-notify/
    main.go

internal/
  input/
  event/
  render/
  notify/
    windows/
  dedupe/
  logging/
  config/
```

职责划分如下：

- `cmd/codex-notify`
  CLI 入口，负责组装依赖和处理退出码

- `internal/input`
  从 `stdin`、参数、文件读取 JSON

- `internal/event`
  解析通用事件信封

- `internal/render`
  已知类型渲染器和默认渲染器

- `internal/notify/windows`
  使用 `go-toast/toast` 发送 Windows 通知

- `internal/dedupe`
  本地去重状态管理

- `internal/logging`
  初始化 `zerolog + lumberjack`

- `internal/config`
  配置加载

## 14. 关键处理流程

建议整体流程如下：

1. 程序启动
2. 初始化日志
3. 读取输入 JSON
4. 解析为 `EventEnvelope`
5. 执行去重判断
6. 根据 `type` 选择 renderer
7. 生成统一 `Notification`
8. 调用 Windows notifier 发送通知
9. 记录成功或失败日志
10. 返回对应退出码

## 15. 失败处理策略

### 15.1 非法 JSON

- 记录错误日志
- 返回退出码 `2`
- 不发送通知

### 15.2 未知类型

- 不视为错误
- 使用默认 renderer
- 返回退出码 `0`

### 15.3 通知发送失败

- 记录错误日志
- 返回退出码 `1`

### 15.4 去重命中

- 记录 info 日志
- 不重复发送通知
- 返回退出码 `0`

## 16. 第一阶段实现范围

第一阶段建议只完成以下能力：

- 从 `stdin` 和 `--payload` 读取 JSON
- 解析通用 `EventEnvelope`
- 支持 `agent-turn-complete` 的专用通知模板
- 支持未知 `type` 的默认通知模板
- 使用 `go-toast/toast` 发送 Windows 通知
- 使用 `zerolog + lumberjack` 自动落盘日志
- 实现基于 `turn-id` 的基础去重

## 17. 暂不纳入第一阶段的能力

以下内容先不做，以保证第一版足够稳定和简单：

- 通知点击后执行命令
- 托盘常驻进程
- HTTP 服务模式
- 多通知渠道同时发送
- 复杂模板语言
- 丰富交互按钮工作流

## 18. 后续扩展方向

本方案后续可以自然扩展为：

- 新增事件模板配置
- 点击通知后打开 `cwd`
- 点击通知后触发 `codex resume`
- 扩展到企业微信、飞书、邮件等渠道
- 支持守护进程模式
- 支持集中式事件收集与通知统计

## 19. 最终结论

本项目应被设计为一个“通用事件通知网关”，而不是只服务于某一类固定 JSON 的一次性脚本。

技术选型如下：

- 输入方式：`stdin` 为主，`--payload` 为辅
- 事件模型：通用 `EventEnvelope`，保留完整原始 JSON
- 通知渲染：已知类型专用模板，未知类型默认模板
- 通知发送：`github.com/go-toast/toast`
- 日志方案：`rs/zerolog + lumberjack`
- 状态存储：`%LOCALAPPDATA%\codex-notify\state\`

该方案兼顾了以下目标：

- 当前需求可快速落地
- 未知事件类型可持续兼容
- Windows 长期本地使用稳定
- 后续实现和替换成本可控
